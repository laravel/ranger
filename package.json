{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "test": "APP_ENV=testing ./vendor/bin/testbench ranger:generate --path=workbench/resources/js/ranger --throw && oxlint ./workbench/resources/js && oxlint ./tests && vitest run --typecheck"
    },
    "devDependencies": {
        "happy-dom": "^18.0.1",
        "oxlint": "^1.0.0",
        "vite": "^6.0",
        "vitest": "^3.2.3"
    }
}
